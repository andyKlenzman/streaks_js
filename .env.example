# ABOUTME: Example environment variables for local development.
# ABOUTME: Copy to .env and adjust values as needed.

# DB source: "firebase" (default) or "browser" (local localStorage, no Firebase needed)
VITE_DB_SOURCE=firebase
